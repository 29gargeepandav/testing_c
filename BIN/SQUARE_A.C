#include<stdio.h>
#include<conio.h>

void main()
{
	int a, area;  // a is side
	clrscr();
	printf("\nenter of value side:");
	scanf("%d", &a);

	area=a*a;
	printf("\n\n area of square is %d", area);
	getch();
}
