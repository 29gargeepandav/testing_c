#include<stdio.h>
#include<conio.h>

void main()
{
	int l,w, area;
	clrscr();
	printf("enter length :");
	scanf("%d", &l);

	printf("enter width :");
	scanf("%d", &w);

	area=l*w;
	printf("\n\n area of recangle is %d", area);
	getch();
}
